#!/bin/bash
#rofi -modi " :greenclip print" -show " " -run-command '{cmd}' -theme ~/.config/rofi/launchers/misc/blurry.rasi
rofi -modi " :greenclip print" -show " " -run-command '{cmd}' -theme /usr/share/rofi/themes/blurry_noicon.rasi
