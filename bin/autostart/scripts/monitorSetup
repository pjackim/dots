#! /bin/python3

import os

os.system('xrandr --current | grep " connected" > ../logs/monitor_info')

screens = []
file = open("../logs/monitor_info")
for line in file:
    screens.append(line.split(" ")[0])


